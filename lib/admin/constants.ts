/**
 * Constantes partagées sans dépendance Node (importables depuis l'Edge runtime,
 * ex. middleware.ts). Garder ce fichier exempt de `node:*`.
 */
export const SESSION_COOKIE = "labella_admin_session";
